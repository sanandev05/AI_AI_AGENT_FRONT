export const metadata = {
  title: "Manus - Home",
  description: "Welcome to Manus — AI workspace and assistant",
};

import HomeClient from './HomeClient'

export default function Page(){
  return <HomeClient />
}
